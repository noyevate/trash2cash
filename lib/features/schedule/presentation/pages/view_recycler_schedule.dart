import 'package:flutter/material.dart';

class ViewRecyclerSchedulePage extends StatelessWidget {
  const ViewRecyclerSchedulePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
      backgroundColor: Colors.white,
      body: SafeArea(
        child: Column(),
      )
    );
  }
}